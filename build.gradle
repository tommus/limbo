//region Build Script

buildscript {

  //region Versioning

  ext.LIMBO_VERSION = "0.5.5"
  ext.LIMBO_VERSION_CODE = 9

  //endregion

  //region Repositories

  repositories {
    apply from: "versions.gradle"
    addRepos(repositories)
  }

  //endregion

  //region Dependencies

  dependencies {
    classpath deps.build.android_gradle_plugin
  }

  //endregion
}

//endregion

//region All Projects

allprojects {
  repositories {
    addRepos(repositories)
  }
}

//endregion

//region Sub Projects

subprojects { project ->
  group = GROUP
  version = VERSION_NAME
}

//endregion

//region Tasks

task clean(type: Delete) {
  delete rootProject.buildDir
}

//endregion
